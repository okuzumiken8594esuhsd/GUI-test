README

Each of the files is meant to run on its own.
Each file has a compressed version and an uncompressed version

I think that it may have been easier to make my own classes and use inheritance to not have to redo lots of code on making the frames and adding buttons, etc.